# Site URL — OBLIGATOIRE EN PRODUCTION (ex: https://chocolat-cedric-brun.fr)
# En dev : http://localhost:3000 (ou laisser vide, les headers de requête seront utilisés)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://ofyfgzntvcbketivoyxz.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_...
# Clé service_role (dashboard Supabase → Project Settings → API) — permet d'afficher un lien de réinitialisation de mot de passe sur la page (évite les liens non cliquables dans l'email)
SUPABASE_SERVICE_ROLE_KEY=

# Email (Resend) — confirmation de commande, demande de commande pro, notification propriétaire
# Créer un compte sur resend.com, vérifier un domaine puis copier la clé API
RESEND_API_KEY=re_...
# Adresse expéditrice (doit être sur un domaine vérifié Resend, ou utiliser onboarding@resend.dev pour les tests)
EMAIL_FROM=Cédric Brun <noreply@votredomaine.fr>
# Adresse qui reçoit les demandes de commande pro (formulaire page /pro). Répondre à l'email renverra au client.
DEMANDE_COMMANDE_EMAIL=contact@cedric-brun.com
# Adresse qui reçoit une notification à chaque nouvelle commande (client, livraison, facturation, produits)
ORDER_NOTIFICATION_EMAIL=patisseriebrun-25@orange.fr

# INSEE API Sirene — validation SIRET pour l'accès aux pages Pro / Prix
# Clé gratuite : https://api.insee.fr/catalogue/ → créer un compte, s'abonner à l'API Sirene, générer un token
INSEE_SIRENE_API_TOKEN=
